import * as React from 'react';
import { shallow } from 'enzyme';
import * as stubbedModel from './stubbedResponse';
import { ISPComponent } from '../Scripts/ItemsSearchParams';

describe('ItemsSearchParams', () => {
    test('renders', () => {
        expect('this needs to be done').toBeDefined();
    });
});
